# This repo is a public GitHub Pages site. Only the encrypted BLOB inside the
# dashboard pages may carry business figures — never a plaintext payload.
# The QuickBooks snapshots normally live outside the repo, in
# ~/Dropbox/Anomaly Creative LLC/qbo-snapshots/; these patterns are a backstop
# in case a copy is ever dropped in here by hand.
financials_payload.json
qbo-snapshots/
pl_*.json
extra.json

.DS_Store
